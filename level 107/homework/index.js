// modularity - is a design principle used in software engineering (and other fields) that involves breaking a system into smaller, self-contained, and independent parts called modules. Each module is responsible for a specific piece of functionality and can be developed, tested, and maintained separately.

// key attribute - in React is a special attribute used to help React identify which items in a list have changed, been added, or removed. It improves rendering performance and helps React efficiently update the UI.


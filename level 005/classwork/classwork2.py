snake_case = "gveli_shxamiania"

# In coding we use snake_case for naming values because we can't use spaces.

print(snake_case)

# Print(snake_case) would be error, because python is case sensitive language, which means that only lower case letters are used for functions. 

# and we use comments for other programmers or ourselves to understand code we write or not to confuse it with something else.
// jsx is js syntax extenension to xml which looks like modern html code. jsx allows us to use both js and html syntax to make writing code easier.

// VirtualDOM (VDOM) is better than regular DOM because it is more efficient. regular DOM repaints whole site while VDOM only takes part that is changed than updates it, by comparing new result to old result and then renders it.
